Upload all files in this package to the root of your M3-GameBoard repo.

This version is a controller-only cleanup:
- simpler layout
- dropdown fallback for saved paths
- exact icon folder mapping for assets/icons/
